{
  "name": "bnn-server",
  "version": "1.0.0",
  "description": "Backend server of BNN social app",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "express": "^4.21.1",
    "nodemon": "^3.1.7",
    "typescript": "^5.6.3"
  }
}
